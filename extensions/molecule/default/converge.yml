---
- name: Converge
  hosts: all
  gather_facts: false
  tasks:
    - name: Testing role
      ansible.builtin.include_role:
        # name: matcai_roles.openvpn
        name: ansible-role-openvpn
        tasks_from: main.yml
  # tasks:
  #   - name: Replace this task with one that validates your content
  #     ansible.builtin.debug:
  #       msg: "This is the effective test"
